@model StopModel

@{
	var exists = Model != null && Model.StopId != 0;
	ViewBag.Title = (exists ? "Редактирование" : "Создание") + " Stop";
}

<form asp-action="Update" asp-antiforgery="true" method="post">
	<div class="row">
		<div class="col-md-4 col-sm-6">
			<div class="form-group">
				<label asp-for="StopName"></label>
				<input type="text" asp-for="StopName" class="form-control"/>
				<span asp-validation-for="StopName" class="text-danger"></span>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-md-12 text-center">
			<button class="btn btn-outline-primary" type="submit">@(exists ? "Сохранить" : "Добавить")</button>
			<a asp-action="Index" class="btn btn-outline-secondary">Отмена</a>
		</div>
	</div>
</form>
